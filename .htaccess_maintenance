#Down for maintenance page redirect
RewriteEngine On
RewriteBase /
RewriteCond %{REQUEST_URI} !(index\.htm|\.png|\.jpg|\.gif)$
RewriteRule ^(.*)$ http://ajparkes.com.au/index.htm [R=307,L]
#Down for maintenance page redirect
